import XCTest
@testable import ServiceSDKPackage

final class ServiceSDKPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ServiceSDKPackage().text, "Hello, World!")
    }
}
